## What does this change?

## Checklist

- [ ] This PR targets `develop`, not `main`.
- [ ] `cargo fmt --check` passes.
- [ ] `cargo clippy` passes.
- [ ] `cargo test` passes.
- [ ] A `CHANGELOG.md` entry is added, if this change is user-visible.
- [ ] Docs are updated, if this change touches configuration surface — and the [two-register rule](../CONTRIBUTING.md#documentation-style-two-registers) is respected (terse tables for reference sections, prose for everything else).
